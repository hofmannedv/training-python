README
------

* comparing-strings.py: demonstrates how to compare strings using 
  the == operator

* comparing-strings-order.py: demonstrates how to compare strings
  according to lexicographical order based on the < and > operators
